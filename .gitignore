# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# codegraph (local code index + editor rules)
.codegraph/
.cursor/

# supabase CLI scratch
supabase/.temp/

# Local email template previews
emails-preview/

# Remotion demo-video project — separate toolchain with its own package.json and
# node_modules; not part of the Next build. Keep it out of the web repo (init it
# as its own git repo if you want history).
remotion-skillme-ai-engineer/
